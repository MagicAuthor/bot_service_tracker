1. В файле .env задается API-токен бота и id администраторов (id через запятую, без пробелов).
2. Для работы боту необходимо предоставить доступ на использование системных вызовов systemctl (sudoers).